import random
import string

def generate_password(length=12):
    characters = string.ascii_letters + string.digits  # Includes uppercase, lowercase, and numbers
    password = ''.join(random.sample(characters, length))  # Randomly selects characters
    return password

print("Generated password:", generate_password())
